kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: 'my-egress-nw-policy'
  namespace: '{{ meta.namespace }}'
spec:
  podSelector: 
    matchLabels:
      app: my-app-name
  egress:
    - {}
  policyTypes:
    - Egress
