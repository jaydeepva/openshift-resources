kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: 'my-nw-policy'
  namespace: '{{ meta.namespace }}'
spec:
  podSelector: 
    matchLabels:
      app: my-app-name
  ingress:
    - {}
  policyTypes:
    - Ingress
