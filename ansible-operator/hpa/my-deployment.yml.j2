kind: Deployment
apiVersion: apps/v1
metadata:
  name: my-deployment
  namespace: '{{ meta.namespace }}'
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: my-container-name
        image: 'my-image-repo/my-image'
