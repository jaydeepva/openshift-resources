apiVersion: v1
kind: Service
metadata:
  name: 'my-service-b'
  namespace: '{{ meta.namespace }}'
  labels:
    app: my-serviceb
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: serviceb-certs
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      name: serviceb
  selector:
	...
